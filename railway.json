{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "nixpacksConfig": {
            "providers": ["php", "node"],
            "buildCmd": "npm install && npm run build && composer install --no-dev --optimize-autoloader"
        }
    },
    "deploy": {
        "startCommand": "touch database/database.sqlite && php artisan migrate --force && php artisan db:seed && php artisan serve --host 0.0.0.0 --port $PORT",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
